import React from 'react'
import Layout from '../../components/Layout'
import {Container, Divider, Grid, Section, Header, Message } from 'semantic-ui-react'

const SuccessPage = () => (
    <Layout>
        <Grid centered>
            <Container>
                Thanks for reaching out!
                Click here to return to the home page or something like that
                What does netlify's success message look like?
            </Container>
        </Grid>
    </Layout>
)